import { ScanType } from "@repo/shared/type"

export class ScanFactory {

    static getScanInstance(type: string) {
        switch (type) {
            case ScanType.ADMIN_SECURITY : {
                
            }
        }
    }

}           