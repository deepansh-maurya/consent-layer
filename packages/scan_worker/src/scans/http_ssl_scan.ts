    /**\
     * 
     * 6. HTTP_SSL
 Check if site is using HTTPS

 Test if HTTP automatically redirects to HTTPS

 Detect presence of HSTS headers, certificate validity, expiration

 Scan all important pages for mixed content (HTTP resources on HTTPS pages)

 Save: status per page, HSTS status, cert info, mixed content findings

 Metrics, job timeouts (shared)
     */

 export default class HTTP_SSL{}